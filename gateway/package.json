{
  "name": "@iris/gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The only publicly reachable API process. Verifies who is calling, throttles them, proxies to core-service. Deliberately thin.",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/static": "^8.0.3",
    "@iris/shared": "*",
    "fastify": "^5.2.0",
    "ioredis": "^5.4.2",
    "jose": "^5.9.6",
    "pg": "^8.13.1",
    "pino": "^9.5.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "pino-pretty": "^13.0.0"
  }
}
