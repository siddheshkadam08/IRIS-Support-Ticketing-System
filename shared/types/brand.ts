/**
 * Brand constants.
 *
 * BRAND_FOOTER is the single source of truth for the footer that must appear
 * throughout the application — the widget (every view), the demo page, and any
 * future admin-panel screen. Import it; never retype the string. A footer that
 * appears in two places as two literals will disagree within a week.
 */
export const BRAND_FOOTER = 'Powered by Elevate - X.';

/** Shown under the widget composer, above the footer. */
export const AI_DISCLAIMER = 'AI can make mistakes. Please verify important information.';

export const PLATFORM_NAME = 'IRIS Support';
