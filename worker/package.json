{
  "name": "@iris/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The async engine. Consumes the single ai.jobs queue and orchestrates Core and the AI service. Holds no database credential and makes no business decisions.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "backfill:embeddings": "tsx src/embedding-backfill.ts"
  },
  "dependencies": {
    "@iris/shared": "*",
    "bullmq": "^5.34.0",
    "ioredis": "^5.4.2",
    "pino": "^9.5.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  }
}
